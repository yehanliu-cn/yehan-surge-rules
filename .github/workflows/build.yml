name: Build Rules

on:
  workflow_dispatch:    # 手动触发
  push:                 # 只要有提交就触发
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: List all files for debug
        run: ls -lah

      # 如需自动生成或转换，可以在这里加shell/python命令
      # - name: Convert AdGuard to Surge rules (example, delete if not needed)
      #   run: |
      #     bash script/my_convert_script.sh

      # 如需要上传产物，可以加下面（示例）：
      # - name: Upload result
      #   uses: actions/upload-artifact@v3
      #   with:
      #     name: rules
      #     path: ./output/
